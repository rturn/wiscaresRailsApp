<p id="notice"><%= notice %></p>

<h1>Listing Health Problems</h1>

<table>
  <thead>
    <tr>
      <th>Dateentered</th>
      <th>Petid</th>
      <th>Description</th>
      <th>Dateoccurred</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @health_problems.each do |health_problem| %>
      <tr>
        <td><%= health_problem.dateEntered %></td>
        <td><%= health_problem.petID %></td>
        <td><%= health_problem.description %></td>
        <td><%= health_problem.dateOccurred %></td>
        <td><%= link_to 'Show', health_problem %></td>
        <td><%= link_to 'Edit', edit_health_problem_path(health_problem) %></td>
        <td><%= link_to 'Destroy', health_problem, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Health problem', new_health_problem_path %>
